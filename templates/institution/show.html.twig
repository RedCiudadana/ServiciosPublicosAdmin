{% extends '_main.html.twig' %}

{% block title %}Institution{% endblock %}

{% block body %}
    <h1>Institution</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ institution.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ institution.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_institution_index') }}">{% trans %}back to list{% endtrans %}</a>

    <a href="{{ path('app_institution_edit', {'id': institution.id}) }}">{% trans %}edit{% endtrans %}</a>

    {{ include('institution/_delete_form.html.twig') }}
{% endblock %}
