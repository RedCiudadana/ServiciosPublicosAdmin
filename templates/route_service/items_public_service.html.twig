{% extends '_main.html.twig' %}

{% import "_table.html.twig" as table %}
{% import "_buttons.html.twig" as buttons %}

{% block title %}{% trans %}Rutas de servicios{% endtrans %}{% endblock %}

{% block body %}
    <div class="col-lg-12">
        <h1 class="font-sans px-8 py-4 rounded mt-0 mb-4 bg-white shadow-sm text-xl font-bold">
            {% trans %}Agregar requerimiento para {% endtrans %} {{publicService.name}}
        </h1>
    </div>
    <div class="col-lg-12">
        <div class="px-8 py-4 rounded mt-0 mb-4 bg-white shadow-sm text-xl font-bold">
            {{ form_start(form) }}
                {{ form_widget(form) }}
                <button class="bg-blue-700 text-white px-3 py-2 rounded font-bold text-xs mb-4">
                    {{ button_label|default('Agregar requerimiento') }}
                </button>
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}